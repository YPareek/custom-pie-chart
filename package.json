{
  "name": "piechartwc",
  "version": "1.0.0",
  "description": "A pie chart web component, whose size and other functionality is configurable.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yash pareek",
  "license": "ISC",
  "dependencies": {
    "lite-server": "^2.5.3"
  }
}
